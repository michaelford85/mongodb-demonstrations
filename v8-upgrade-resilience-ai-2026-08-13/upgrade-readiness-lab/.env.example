# ─────────────────────────────────────────────
# Upgrade Readiness Lab — environment
#
# Assumes a cluster already provisioned by ../../atlas-cluster-provisioning.
# Every script here is read-only; nothing provisions, upgrades, or writes.
# ─────────────────────────────────────────────

# ─────────────────────────────────────────────
# MongoDB Atlas connection
# Grab the SRV string from the atlas-cluster-provisioning Terraform output
# (connection_strings.standard_srv) and add your user / password.
# A read-only user is enough. Add clusterMonitor if you want $indexStats
# access counts and the feature compatibility version to resolve.
# ─────────────────────────────────────────────
MONGODB_URI="mongodb+srv://username:password@your-cluster.mongodb.net/?retryWrites=true&w=majority"

# OR X.509 certificate auth — comment the line above and use both of these:
# MONGODB_URI="mongodb+srv://your-cluster.mongodb.net/?authSource=%24external&authMechanism=MONGODB-X509&retryWrites=true&w=majority"
# MONGODB_CERT="/path/to/your/cert.pem"

# ─────────────────────────────────────────────
# Optional: comma-separated databases to inspect.
# Leave unset to inspect every non-system database. Overridden by --db.
# ─────────────────────────────────────────────
# DB_NAMES=your_database,another_database
