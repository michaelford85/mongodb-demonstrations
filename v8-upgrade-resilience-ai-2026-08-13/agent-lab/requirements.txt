pymongo>=4.7.0
python-dotenv>=1.0.0
openai>=1.30.0     # optional — only for LLM_PROVIDER=openai
anthropic>=0.34.0  # optional — only for LLM_PROVIDER=anthropic
voyageai>=0.3.0    # optional — only for EMBEDDING_PROVIDER=voyage
